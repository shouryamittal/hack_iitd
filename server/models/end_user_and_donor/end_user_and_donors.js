// === API end points for end user ==== //
import schema from '../../connection';
const router = require('express').Router();

const end_user = schema.end_user;
const donors = schema.donors;

module.exports = router;


